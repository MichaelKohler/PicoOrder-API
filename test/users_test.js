var should = require('should');

describe('Users', function() {
    describe('create user',function() {
        it('should create a user foo with password bar', function(done) {
            var test = true;
            test.should.be.equal(true);
            done();
        });
    });
});